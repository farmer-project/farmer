'use strict';

var Q = require('q');

function Zip() {
}

Zip.prototype.unCompress = function () {
    var deferred = Q.defer();

    return deferred.promise;
};

Zip.prototype.compress = function (address) {
    var deferred = Q.defer();

    return deferred.promise;
};

